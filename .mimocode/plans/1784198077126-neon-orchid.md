# План рефакторинга BallonsTranslator UI — Текущий статус

## Цель
Уменьшить связанность кода, применить принципы SOLID и паттерн MVP (Model-View-Presenter).

**Правило из mvp.txt:**
```
Model < Presenter > View (Interface) < View (PyQt)
```

View зависит ТОЛЬКО от Presenter Interface, НЕ от Model.

---

## Выполнено

### Фаза 1: Извлечение из SceneTextManager ✓

| Компонент | Файл | Строк | Тесты |
|-----------|------|-------|-------|
| TextLayoutManager | `text_layout_manager.py` | 396 | ✓ |
| OverlapResolver | `overlap_resolver.py` | 133 | 5 |
| TransPairWiring | `transpair_wiring.py` | 69 | - |

### Фаза 2: Извлечение из TextBlkItem ✓

| Компонент | Файл | Строк | Тесты |
|-----------|------|-------|-------|
| TextFormatManager | `text_format_manager.py` | 294 | 6 |
| FlowBoundaryManager | `flow_boundary_manager.py` | 295 | 7 |

### Фаза 3: Разделение MainWindow ✓

| Компонент | Файл | Строк | Тесты |
|-----------|------|-------|-------|
| PageManager | `page_manager.py` | 175 | 4 |
| PipelineCoordinator | `pipeline_coordinator.py` | 195 | 3 |
| FileIOManager | `file_io_manager.py` | 130 | 2 |

### Фаза 4: MVP Presenter'ы ✓ (только Presenter без View Interfaces)

| Компонент | Файл | Строк | Тесты |
|-----------|------|-------|-------|
| TextBlockPresenter | `text_block_presenter.py` | 195 | 4 |
| ScenePresenter | `scene_presenter.py` | 165 | 4 |
| FlowPresenter | `flow_presenter.py` | 175 | 3 |

### Исправление багов ✓
1. **MoveBlkItemsCommand** — исправлен скачок текста при перемещении
2. **setFontSize** — исправлено сохранение шрифта через панель
3. **LaMa inpainter** — исправлена ошибка BFloat16/Float

### Фаза 5: Разделение SceneTextManager ✓

| Компонент | Файл | Строк | Тесты |
|-----------|------|-------|-------|
| BlockManager | `block_manager.py` | 175 | ✓ |
| ClipboardManager | `clipboard_manager.py` | 115 | ✓ |
| SelectionManager | `selection_manager.py` | 130 | ✓ |

### Фаза 6: View Interfaces ✓

| Компонент | Файл | Описание |
|-----------|------|----------|
| ITextBlockView | `view_interfaces.py` | Интерфейс для TextBlkItem |
| ISceneView | `view_interfaces.py` | Интерфейс для Canvas |
| IFlowBlockView | `view_interfaces.py` | Интерфейс для FlowTextBlkItem |

**Обновлены Presenters:**
- TextBlockPresenter: зависит от ITextBlockView
- ScenePresenter: зависит от ISceneView
- FlowPresenter: зависит от IFlowBlockView

### Фаза 7: Уменьшение God Classes (Частично)

| Компонент | Файл | Описание |
|-----------|------|----------|
| KeyboardShortcutManager | `keyboard_shortcut_manager.py` | Горячие клавиши |
| TranslationPipeline | `translation_pipeline.py` | Бизнес-логика перевода |
| ProjectManager | `project_manager.py` | Управление проектами |
| CanvasUndoManager | `canvas_undo_manager.py` | Управление undo/redo |
| LayerCompositor | `layer_compositor.py` | Композиция слоёв |

---

## Осталось сделать

> **Статус:** ВСЕ ФАЗЫ 1-13 ВЫПОЛНЕНЫ (36+ файлов, 147 тестов).
> DI-инфраструктура **полностью интегрирована**:
> - `launch.py` использует `CompositionRoot.build()`
> - `Canvas.__init__` принимает DI-сервисы + прокси-методы
> - `SceneTextManager` делегирует `push_undo_command` через DI UndoManager
> - `MainWindow` создаёт `MainPresenter`, делегирует `OpenProj`/`manual_save`
> - `EventBus` расширен + Canvas публикует SCALE_CHANGED/SELECTION_CHANGED
> - `bottomBar.slider → canvas.method` заменено на EventBus TRANSPARENCY_CHANGED
> - `ScenePresenter` интегрирован с EventBus (DELETE/COPY/PASTE_TEXT_BLOCKS)

---

### Фаза 11: Интеграция DI + MainPresenter

#### Шаг 11.1: Canvas принимает DI-сервисы

**Файл**: `ui/canvas.py` — изменить `__init__`

**Текущий** (строка 200):
```python
def __init__(self, parent=None):
```

**Новый**:
```python
def __init__(self, undo_manager=None, layer_manager=None, scale_manager=None, 
             clipboard_service=None, parent=None):
    super().__init__(parent)
    self._undo_mgr = undo_manager
    self._layer_mgr = layer_manager
    self._scale_mgr = scale_manager
    self._clipboard_svc = clipboard_service
    # ... остальная инициализация
```

**Добавить прокси-методы** (для обратной совместимости, пока все потребители не обновятся):
```python
def undo(self):
    if self._undo_mgr:
        return self._undo_mgr.undo()
    return self.draw_undo_stack.undo()

def redo(self):
    if self._undo_mgr:
        return self._undo_mgr.redo()
    return self.draw_undo_stack.redo()

def push_undo_command(self, cmd):
    if self._undo_mgr:
        return self._undo_mgr.push_command(cmd)
    self.draw_undo_stack.push(cmd)

def clear_undostack(self, update_saved_step=False):
    if self._undo_mgr:
        self._undo_mgr.clear()
    else:
        self.draw_undo_stack.clear()
        self.text_undo_stack.clear()
    if update_saved_step:
        self.update_saved_undostep()

def updateLayers(self):
    if self._layer_mgr:
        return self._layer_mgr.update_layers()
    # ... существующая логика

def scaleImage(self, factor):
    if self._scale_mgr:
        return self._scale_mgr.set_scale(factor)
    # ... существующая логика

def on_paste(self, pos):
    if self._clipboard_svc:
        return self._clipboard_svc.paste_blocks(pos)
    # ... существующая логика с self.clipboard_blks
```

**Файл**: `ui/canvas.py` — изменить конструктор CustomGV

Текущий код (строка 209): `self.gv = CustomGV(self)` — остаётся без изменений.

**Ключевой момент:** Canvas создаёт свои слои (`baseLayer`, `inpaintLayer`, `drawingLayer`, `textLayer`) и UndoStacks внутри `__init__`. При DI-интеграции:
- Если DI-сервис передан → использовать его
- Если нет → создать свои (обратная совместимость)

---

#### Шаг 11.2: CompositionRoot интегрируется в launch.py

**Файл**: `launch.py` — изменить строки 279-281

**Было**:
```python
from ui.mainwindow import MainWindow
ballontrans = MainWindow(app, config, open_dir=args.proj_dir, **vars(args))
```

**Стало**:
```python
from ui.composition_root import CompositionRoot
ballontrans = CompositionRoot.build(config, app, open_dir=args.proj_dir, **vars(args))
```

**Файл**: `ui/composition_root.py` — исправить `build()` метод

Текущая ошибка: `build()` передаёт `canvas=canvas` в `MainWindow`, но `MainWindow.__init__` не принимает этот параметр.

**Новый `build()`**:
```python
@staticmethod
def build(config, app, open_dir='', **exec_args):
    from utils import shared
    from .event_bus import EventBus

    DIContainer.reset()

    event_bus = EventBus.get_instance()
    DIContainer.register(EventBus, event_bus)

    proj = ProjImgTrans()
    DIContainer.register(ProjImgTrans, proj)

    from .undo_manager import UndoManager
    from .layer_manager import LayerManager
    from .clipboard_service import ClipboardService
    from .scale_manager import ScaleManager

    undo_mgr = UndoManager()
    layer_mgr = LayerManager()
    clipboard_svc = ClipboardService()
    scale_mgr = ScaleManager()

    DIContainer.register(UndoManager, undo_mgr)
    DIContainer.register(LayerManager, layer_mgr)
    DIContainer.register(ClipboardService, clipboard_svc)
    DIContainer.register(ScaleManager, scale_mgr)

    from .mainwindow import MainWindow
    # MainWindow создаёт Canvas внутри setupUi(), 
    # передаём сервисы через exec_args или устанавливаем после создания
    main_window = MainWindow(app, config, open_dir=open_dir, **exec_args)
    
    # Устанавливаем DI-сервисы после создания
    if hasattr(main_window, 'canvas'):
        main_window.canvas._undo_mgr = undo_mgr
        main_window.canvas._layer_mgr = layer_mgr
        main_window.canvas._scale_mgr = scale_mgr
        main_window.canvas._clipboard_svc = clipboard_svc

    return main_window
```

**Примечание:** Canvas создаётся внутри `MainWindow.setupUi()`, поэтому CompositionRoot не может передать сервисы через конструктор. Вместо этого сервисы устанавливаются после создания.

---

#### Шаг 11.3: Создать MainPresenter

**Файл**: `ui/main_presenter.py` (~250 строк)

```python
"""
MainPresenter — Presenter для MainWindow.

Выделяет бизнес-логику из MainWindow (MVP: Model > Presenter > View).
"""

from typing import Optional, TYPE_CHECKING
from qtpy.QtCore import QObject
import os.path as osp

from utils.logger import logger as LOGGER
from utils.config import pcfg
from .event_bus import EventBus, Events

if TYPE_CHECKING:
    from .mainwindow import MainWindow
    from utils.proj_imgtrans import ProjImgTrans


class MainPresenter(QObject):
    """
    Presenter для MainWindow — бизнес-логика.
    
    Responsibilities:
    - Навигация по страницам
    - Сохранение проекта
    - Pipeline перевода
    - Управление проектом
    """

    def __init__(self, model: 'ProjImgTrans', view: 'MainWindow', event_bus: EventBus):
        super().__init__()
        self._model = model
        self._view = view
        self._event_bus = event_bus
        self._subscribe_to_events()

    def _subscribe_to_events(self):
        """Подписка на события EventBus."""
        self._event_bus.subscribe(Events.UNDO, self._on_undo)
        self._event_bus.subscribe(Events.REDO, self._on_redo)

    def open_project(self, dir_path: str):
        """Открывает проект из директории."""
        try:
            self._view.opening_dir = True
            self._model.load(dir_path)
            self._view.st_manager.clearSceneTextitems()
            self._view.titleBar.setTitleContent(osp.basename(dir_path))
            self._view.updatePageList()
            self._view.opening_dir = False
            self._event_bus.publish(Events.PROJECT_OPENED, dir_path)
        except Exception as e:
            self._view.opening_dir = False
            from utils.message import create_error_dialog
            create_error_dialog(e, self._view.tr('Failed to load project ') + dir_path)

    def save_current_page(self, update_scene_text=True, save_proj=True, 
                          restore_interface=False, save_rst_only=False, 
                          keep_exist_as_backup=False):
        """Сохраняет текущую страницу."""
        # Делегируем в MainWindow (сложная UI-логика)
        self._view.saveCurrentPage(
            update_scene_text=update_scene_text,
            save_proj=save_proj,
            restore_interface=restore_interface,
            save_rst_only=save_rst_only,
            keep_exist_as_backup=keep_exist_as_backup
        )

    def on_page_changed(self, page_idx: int):
        """Обработчик смены страницы."""
        self._view.pageListCurrentItemChanged()

    def _on_undo(self, data=None):
        """Обработчик undo."""
        if hasattr(self._view, 'canvas'):
            self._view.canvas.undo()

    def _on_redo(self, data=None):
        """Обработчик redo."""
        if hasattr(self._view, 'canvas'):
            self._view.canvas.redo()

    def manual_save(self):
        """Ручное сохранение."""
        if self._view.leftBar.imgTransChecker.isChecked() \
            and self._model.directory is not None:
            LOGGER.debug('Manually saving...')
            self.save_current_page(update_scene_text=True, save_proj=True, 
                                  restore_interface=True, save_rst_only=False)
```

---

#### Шаг 11.4: Интегрировать MainPresenter в MainWindow

**Файл**: `ui/mainwindow.py` — изменить `__init__` (строка 101)

**Добавить** после `self.setupRegisterWidget()` (строка 118):
```python
# Создаём MainPresenter
from .main_presenter import MainPresenter
from .event_bus import EventBus
event_bus = EventBus.get_instance()
self._presenter = MainPresenter(self.imgtrans_proj, self, event_bus)
```

**Добавить свойство** для доступа к presenter:
```python
@property
def presenter(self):
    return self._presenter
```

**Изменить** `OpenProj()` (строка 493) для делегирования:
```python
def OpenProj(self, proj_path: str):
    if hasattr(self, '_presenter'):
        self._presenter.open_project(proj_path)
    else:
        # Fallback для обратной совместимости
        if osp.isdir(proj_path):
            self.openDir(proj_path)
        else:
            self.openJsonProj(proj_path)
        
        if pcfg.let_textstyle_indep_flag and not shared.HEADLESS:
            self.load_textstyle_from_proj_dir(from_proj=True)
```

**Изменить** `manual_save()` (строка 925):
```python
def manual_save(self):
    if hasattr(self, '_presenter'):
        self._presenter.manual_save()
    else:
        # Fallback
        if self.leftBar.imgTransChecker.isChecked()\
            and self.imgtrans_proj.directory is not None:
            LOGGER.debug('Manually saving...')
            self.saveCurrentPage(update_scene_text=True, save_proj=True, 
                               restore_interface=True, save_rst_only=False)
```

---

### Фаза 12: Расширить EventBus

#### Шаг 12.1: Добавить новые события

**Файл**: `ui/event_bus.py` — добавить в класс `Events` (строка 151):

```python
# Страницы (продолжение)
PAGE_CHANGING = "page_changing"      # Начало смены страницы

# Проект
PROJECT_OPENED = "project_opened"    # Проект открыт
PROJECT_CLOSED = "project_closed"    # Проект закрыт
PROJECT_SAVED = "project_saved"      # Проект сохранён

# Режимы
MODE_CHANGED = "mode_changed"        # Смена режима (textedit/draw/textblock)

# Undo/Redo (уже есть: UNDO, REDO)
```

---

### Фаза 13: Тесты для DI и MainPresenter

#### Тест 13.1: test_di_container.py

**Файл**: `tests/ui/test_di_container.py` (~60 строк)

```python
"""Tests for DIContainer."""
import pytest
from ui.di_container import DIContainer


class TestDIContainer:
    def setup_method(self):
        DIContainer.reset()
    
    def test_register_resolve(self):
        class IMyService:
            pass
        
        class MyService(IMyService):
            pass
        
        service = MyService()
        DIContainer.register(IMyService, service)
        
        resolved = DIContainer.resolve(IMyService)
        assert resolved is service
    
    def test_resolve_unregistered(self):
        class IUnknown:
            pass
        
        with pytest.raises(KeyError):
            DIContainer.resolve(IUnknown)
    
    def test_try_resolve(self):
        class IOptional:
            pass
        
        assert DIContainer.try_resolve(IOptional) is None
    
    def test_is_registered(self):
        class ICheck:
            pass
        
        assert not DIContainer.is_registered(ICheck)
        DIContainer.register(ICheck, object())
        assert DIContainer.is_registered(ICheck)
    
    def test_reset(self):
        class IReset:
            pass
        
        DIContainer.register(IReset, object())
        DIContainer.reset()
        assert not DIContainer.is_registered(IReset)
    
    def test_get_all(self):
        class IA:
            pass
        class IB:
            pass
        
        a, b = object(), object()
        DIContainer.register(IA, a)
        DIContainer.register(IB, b)
        
        all_deps = DIContainer.get_all()
        assert IA in all_deps
        assert IB in all_deps
        assert all_deps[IA] is a
```

---

#### Тест 13.2: test_main_presenter.py

**Файл**: `tests/ui/test_main_presenter.py` (~120 строк)

```python
"""Tests for MainPresenter."""
import pytest
from unittest.mock import MagicMock, patch
from ui.main_presenter import MainPresenter
from ui.event_bus import EventBus, Events


@pytest.fixture(autouse=True)
def reset_event_bus():
    """Reset EventBus singleton before each test."""
    EventBus.reset_instance()
    yield
    EventBus.reset_instance()


class TestMainPresenter:
    def setup_method(self):
        self.model = MagicMock()
        self.view = MagicMock()
        self.event_bus = EventBus.get_instance()
        self.presenter = MainPresenter(self.model, self.view, self.event_bus)
    
    def test_subscribe_to_events(self):
        assert self.event_bus.has_subscribers(Events.UNDO)
        assert self.event_bus.has_subscribers(Events.REDO)
    
    def test_open_project_calls_model_load(self):
        self.presenter.open_project('/test/dir')
        self.model.load.assert_called_once_with('/test/dir')
    
    def test_open_project_publishes_event(self):
        received = []
        self.event_bus.subscribe(Events.PROJECT_OPENED, lambda d: received.append(d))
        
        self.presenter.open_project('/test/dir')
        assert '/test/dir' in received
    
    def test_manual_save_delegates_to_view(self):
        self.view.leftBar.imgTransChecker.isChecked.return_value = True
        self.model.directory = '/test/dir'
        
        self.presenter.manual_save()
        self.view.saveCurrentPage.assert_called_once()
    
    def test_on_undo_calls_canvas_undo(self):
        self.presenter._on_undo()
        self.view.canvas.undo.assert_called_once()
    
    def test_on_redo_calls_canvas_redo(self):
        self.presenter._on_redo()
        self.view.canvas.redo.assert_called_once()
```

---

### Порядок реализации

| Приоритет | Шаг | Файлы | Эффект |
|-----------|-----|-------|--------|
| 1 | Canvas DI integration | canvas.py | Принимает DI-сервисы |
| 2 | CompositionRoot → launch.py | composition_root.py, launch.py | DI интегрирован |
| 3 | MainPresenter | main_presenter.py + mainwindow.py | MVP для MainWindow |
| 4 | EventBus expansion | event_bus.py | Новые события |
| 5 | DI Container tests | test_di_container.py | +6 тестов |
| 6 | MainPresenter tests | test_main_presenter.py | +6 тестов |
| 7 | Integration test | manual testing | Запуск приложения |

---

### Верификация

```bash
# После каждого изменения:
myenv\Scripts\python -m pytest tests/ui/ -v

# Полный прогон:
myenv\Scripts\python -m pytest -v

# Запуск приложения:
"myenv\Scripts\python.exe" launch.py

# Проверка что DI работает:
myenv\Scripts\python -c "
from ui.di_container import DIContainer
from ui.event_bus import EventBus
from ui.undo_manager import UndoManager
print('DI Container OK:', DIContainer.get_all())
print('EventBus OK:', EventBus.get_instance())
print('UndoManager OK:', UndoManager())
"
```

---

## Метрики

| Метрика | До | Сейчас | Цель |
|---------|-----|--------|------|
| Файлов создано | 0 | 36 | 33 ✓ |
| Тестов | 114 | 147 | 126 ✓ |
| MVP Presenters | 3 | 4 | 4 ✓ |
| DI Services | 4 | 4 | 4 ✓ |
| DI интегрирован | Нет | ✓ | ✓ |
| EventBus events | 21 | 27 | 27 ✓ |
| canvas.gv violations | 72 | 0 | 0 ✓ |
| canvas.textLayer violations | 15 | 0 | 0 ✓ |

---

## Финальная архитектура (после фаз 11-13)

```
CompositionRoot (launch.py)
  ├── DIContainer (register/resolve)
  ├── EventBus (singleton)
  ├── UndoManager (IUndoManager)
  ├── LayerManager (ILayerManager)
  ├── ClipboardService (IClipboardService)
  ├── ScaleManager (IScaleManager)
  │
  ├── MainWindow (View) ──uses──▶ MainPresenter ──uses──▶ ProjImgTrans (Model)
  │     └── Canvas (View) ──delegates──▶ UndoManager, LayerManager, ScaleManager
  │
  └── SceneTextManager ──uses──▶ ScenePresenter, TextBlockPresenter, FlowPresenter
```

**Правило**: View зависит ТОЛЬКО от Presenter Interface. Presenter зависит от Model и View Interface. Model не зависит ни от кого.

---

## Критические файлы для изменений

| Файл | Изменение | Статус |
|------|-----------|--------|
| `ui/canvas.py` | Принимать DI-сервисы в `__init__`, добавить прокси-методы | ✓ |
| `ui/composition_root.py` | Исправить `build()` для интеграции с MainWindow | ✓ |
| `launch.py` | Заменить `MainWindow(...)` на `CompositionRoot.build(...)` | ✓ |
| `ui/main_presenter.py` | **СОЗДАН** — выделяет бизнес-логику из MainWindow | ✓ |
| `ui/mainwindow.py` | Интегрирован MainPresenter, делегирует методы | ✓ |
| `ui/event_bus.py` | Добавлены `PAGE_CHANGING`, `PROJECT_OPENED`, `MODE_CHANGED`, `TRANSPARENCY_CHANGED` | ✓ |
| `ui/scene_presenter.py` | Расширен с DI сервисами + EventBus интеграция | ✓ |
| `ui/scenetext_manager.py` | push_undo_command делегирует через DI UndoManager | ✓ |
| `tests/ui/test_di_container.py` | **СОЗДАН** — тесты DI Container (6 тестов) | ✓ |
| `tests/ui/test_main_presenter.py` | **СОЗДАН** — тесты MainPresenter (6 тестов) | ✓ |
| `tests/ui/test_undo_manager.py` | **СОЗДАН** — тесты UndoManager (8 тестов) | ✓ |
| `tests/ui/test_clipboard_service.py` | **СОЗДАН** — тесты ClipboardService (5 тестов) | ✓ |
| `tests/ui/test_scale_manager.py` | **СОЗДАН** — тесты ScaleManager (8 тестов) | ✓ |
